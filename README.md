# Lven Instant Builds

Public mirror for Lven Instant installer artifacts. Each tagged release
holds the APK / installer for that version.

The source code stays in the private classeve.com repo; only the
compiled binaries land here so download URLs can be shared.
